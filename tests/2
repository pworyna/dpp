import pytest
from fibbonaci import Fibonacci

@pytest.mark.parametrize("example, expected", [
    (0, 0),
    (1, 1),
    (5, 5),
    (10, 55),
    (-1, None)
])
def test_fibbonacci(example, expected):
    result = Fibonacci(example)
    assert result == expected
